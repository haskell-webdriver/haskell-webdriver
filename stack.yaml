
resolver: nightly-2026-04-19

nix:
  enable: false

system-ghc: true

ghc-options:
  "$locals": >-
    -fwrite-ide-info
    -hiedir .hie
    -dumpdir .ghcdump
    -Wredundant-constraints
    -j8

packages:
- '.'
