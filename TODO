for next release

--fine-tune dependencies. network, transformers, transformers-base, monad-control, attoparsec, aeson(?)

--add special web-driver specific FF profile setters, either as special
functions or via documentation

--add opera config

--improve documentation. Document errors better. Provide more
module introductions

--provide examples
  --basic runSession usage
  --intermediate usage
  --explicit waits usage
  --firefox profile usage
  --REPL usage


for 1.0

-- change WDSession to WDSettings, use WDState as internal State type. Use
ReaderT to store WDVersion for use with implicit configurations.

-- allow settings to automatically load drivers. add modules with driver loading
functions

-- allow setting to modify HTTP headers

-- overload WD commands(???)

-- use http-conduit and attoparsec-conduit to stream JSON data. use request methods from http-types
